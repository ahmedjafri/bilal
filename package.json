{
  "name": "bilal",
  "version": "0.1.0",
  "private": true,
  "description": "A low-dependency azan clock optimized for playing the azan reliably every time",
  "scripts": {
    "dev": "poetry run bilal",
    "start": "poetry run bilal",
    "test": "poetry run pytest tests/ -v",
    "test:watch": "poetry run ptw tests/ -- -v",
    "lint": "poetry run ruff check src/ tests/",
    "lint:fix": "poetry run ruff check --fix src/ tests/",
    "format": "poetry run ruff format src/ tests/",
    "format:check": "poetry run ruff format --check src/ tests/",
    "build:wheel": "poetry build",
    "build:docker": "docker build -t bilal .",
    "docker:run": "docker run -p 8080:8080 --device /dev/snd bilal",
    "install": "poetry install",
    "clean": "rm -rf dist/ build/ *.egg-info src/*.egg-info"
  }
}
